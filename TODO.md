# TODO - Add WebView to Home Page

- [x] Install `react-native-webview` via `npx expo install react-native-webview`
- [x] Update `app/index.tsx` to render a full-screen WebView pointing to `https://jntuaceastudents.classattendance.in/`
- [x] Hide the header in `app/_layout.tsx` so the WebView takes the entire screen
- [x] Run `npm run lint` and fix any errors
- [x] Verify task completion
